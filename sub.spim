.text 
main: 
     li $v0, 5  #Entrada dos dados 
     syscall

     move $s0, $v0 #Movendo o dados para $s0

     li $v0, 5 #Entrada de dados do segundo valor
     syscall

     move $s1, $v0 

     sub $t0, $s0, $s1 #Comando para fazer a subtração dos valores

     li $v0, 1      #Impressão do Int
     move $a0, $t0  #Movendo o resultado da sub para $a0
     syscall

     li $v0, 10
     syscall